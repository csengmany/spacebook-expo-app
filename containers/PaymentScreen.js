import React from "react";
import { StyleSheet, View, Image } from "react-native";
import colors from "../assets/colors";
const { red } = colors;

export default function PaymentScreen({ size }) {
    return (
        <View>

        </View>
    );
}
const styles = StyleSheet.create({
    logo: {
        height: 100,
        width: 100,
        fontFamily:"NotoSansBold",
        color:red
    },
});